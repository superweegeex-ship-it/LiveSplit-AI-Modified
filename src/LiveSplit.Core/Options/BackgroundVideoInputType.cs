namespace LiveSplit.Options;

public enum BackgroundVideoInputType
{
    File,
    VlcMrl,
}

public enum BackgroundVideoBlurType
{
    Gaussian = 0,
    Directional = 1,
    Box = 2,
    Rotational = 3,
}
